/** Four kilobytes of memory. */
#define MEMORY_SIZE 4096

/** Exit code for a memory overflow. */
#define BUFFER_OVERFLOW 102

/** Exit code for a memory underflow. */
#define BUFFER_UNDERFLOW 103

#define FONT_INDEX 0x050

/** One byte of memory. */
typedef unsigned char byte;

/** One address in memory, which is two bytes. */
typedef unsigned short address;